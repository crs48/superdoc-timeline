/**
 * @param {Object} options
 * @param {() => SurfaceManager | null | undefined} options.getSurfaceManager
 * @param {() => Editor | null | undefined} options.getActiveEditor
 * @param {() => SuperDocUI | null | undefined} options.getUi
 * @param {() => LinkPopoverResolver | null | undefined} options.getResolver
 * @param {() => HTMLElement | null | undefined} options.getLayerElement
 * @param {(payload: { error: Error, source: string }) => void} options.emitException
 */
export function useLinkPopover({ getSurfaceManager, getActiveEditor, getUi, getResolver, getLayerElement, emitException, }: {
    getSurfaceManager: () => SurfaceManager | null | undefined;
    getActiveEditor: () => Editor | null | undefined;
    getUi: () => SuperDocUI | null | undefined;
    getResolver: () => LinkPopoverResolver | null | undefined;
    getLayerElement: () => HTMLElement | null | undefined;
    emitException: (payload: {
        error: Error;
        source: string;
    }) => void;
}): {
    handleLinkClick: (payload: LinkClickPayload | null | undefined) => void;
    closeCurrentPopover: (reason?: string) => void;
    destroy: () => void;
};
export type LinkPopoverContext = import('../core/types/index.js').LinkPopoverContext;
export type LinkPopoverResolution = import('../core/types/index.js').LinkPopoverResolution;
export type LinkPopoverResolver = import('../core/types/index.js').LinkPopoverResolver;
export type SurfaceManager = import('../core/surface-manager.js').SurfaceManager;
export type SurfaceHandle = import('../core/types/index.js').SurfaceHandle;
export type SurfaceOutcome = import('../core/types/index.js').SurfaceOutcome;
export type SurfaceRequest = import('../core/types/index.js').SurfaceRequest;
export type DirectSurfaceRequest = import('../core/types/index.js').DirectSurfaceRequest;
export type ExternalSurfaceRenderContext = import('../core/types/index.js').ExternalSurfaceRenderContext;
export type DocumentMode = import('../core/types/index.js').DocumentMode;
export type Editor = import('../core/types/index.js').Editor;
export type SuperDocUI = import('../public/ui/types.js').SuperDocUI;
/**
 * A single-block text-selection target in the Document API `query.match`
 * result shape.
 */
export type TextSelectionTarget = {
    kind: "selection";
    start: {
        kind: "text";
        blockId: string;
        offset: number;
    };
    end: {
        kind: "text";
        blockId: string;
        offset: number;
    };
};
/**
 * Block-anchored address carried by a Document API hyperlink record.
 */
export type HyperlinkAddress = {
    anchor?: {
        start?: {
            blockId?: string;
            offset?: number;
        };
        end?: {
            blockId?: string;
            offset?: number;
        };
    } | undefined;
};
/**
 * The subset of a Document API hyperlink record this composable reads. The
 * records come from the async `doc.hyperlinks` facade, so every field is
 * optional here.
 */
export type HyperlinkRecord = {
    rId?: string | undefined;
    text?: string | undefined;
    externalTarget?: string | undefined;
    anchor?: string | undefined;
    targetKind?: string | undefined;
    hyperlinkNodeId?: string | undefined;
    address?: HyperlinkAddress | undefined;
};
/**
 * A block record as read from `doc.blocks.list()`.
 */
export type BlockRecord = {
    ordinal?: number | undefined;
    nodeId?: string | undefined;
};
/**
 * Result of `doc.blocks.list()` as read here.
 */
export type BlocksListResult = {
    blocks?: BlockRecord[] | undefined;
};
/**
 * One item of a `doc.query.match` result as read here.
 */
export type QueryMatchItem = {
    target?: TextSelectionTarget | undefined;
};
/**
 * Result of `doc.query.match` as read here.
 */
export type QueryMatchResult = {
    items?: QueryMatchItem[] | undefined;
};
/**
 * One story entry of a `doc.hyperlinks.list()` result.
 */
export type HyperlinkStory = {
    storyId?: string | undefined;
    hyperlinks?: HyperlinkRecord[] | undefined;
};
/**
 * Result of `doc.hyperlinks.list()` as read here.
 */
export type HyperlinksListResult = {
    stories?: HyperlinkStory[] | undefined;
};
/**
 * Result of `doc.hyperlinks.get()` as read here.
 */
export type HyperlinkGetResult = {
    success?: boolean | undefined;
    hyperlink?: HyperlinkRecord | undefined;
};
/**
 * Result of `doc.bookmarks.get()` as read here (Document API `BookmarkInfo`).
 * `range.from.blockId` is the bookmark's `w14:paraId`, used to find/reveal
 * the target paragraph. `address.story` is populated for non-body
 * bookmarks (omitted for body) — used to scope this fix to body targets,
 * since `revealBodyTarget`/`pageIndexForBodyTarget` are body-only.
 */
export type BookmarkGetResult = {
    address?: {
        story?: {
            kind?: string;
            storyType?: string;
        };
    } | undefined;
    range?: {
        from?: {
            blockId?: string;
        };
    } | undefined;
};
/**
 * The narrow Document API subset this composable reads. The host facade
 * (`Editor['doc']`) carries no typed surface, so the facades and their results
 * are declared structurally here with every field optional; the call sites
 * keep their runtime narrowing (`Array.isArray`, `typeof`, `?.`) before use.
 * The async-capable facades may return promises, so their results are awaited
 * through `Promise.resolve(...)`.
 */
export type LinkPopoverDocumentApi = {
    blocks?: {
        list?: () => BlocksListResult | null | undefined;
    } | null | undefined;
    query?: {
        match?: (query: {
            select: {
                type: "text";
                pattern: string;
                caseSensitive: boolean;
            };
            require: "any";
        }) => QueryMatchResult | null | undefined;
    } | null | undefined;
    hyperlinks?: {
        list?: () => HyperlinksListResult | Promise<HyperlinksListResult | null | undefined> | null | undefined;
        get?: (input: {
            storyId: string;
            hyperlinkNodeId: string;
        }) => HyperlinkGetResult | Promise<HyperlinkGetResult | null | undefined> | null | undefined;
    } | null | undefined;
    bookmarks?: {
        get?: (input: {
            target: {
                kind: "entity";
                entityType: "bookmark";
                name: string;
            };
        }) => BookmarkGetResult | Promise<BookmarkGetResult | null | undefined> | null | undefined;
    } | null | undefined;
};
/**
 * Result of `activeEditor.pageMetrics.revealBodyTarget()` as read here
 * (`V2EditorHostRevealBodyTargetResult`). Only available in editing/
 * suggesting mode — the host rejects with `editing-not-mounted` in
 * viewing mode.
 */
export type RevealBodyTargetResult = {
    status?: "rejected" | "revealed" | undefined;
    reason?: string | undefined;
};
/**
 * The `v2-link-click` payload the shell relays from the v2 host's
 * `onLinkClick` callback.
 */
export type LinkClickPayload = {
    href: string;
    target: string | null;
    rel: string | null;
    tooltip: string | null;
    element: HTMLAnchorElement;
    clientX: number;
    clientY: number;
    documentMode: DocumentMode;
};
/**
 * Hyperlink identity resolved from the Document API for a clicked anchor.
 */
export type ResolvedHyperlinkTarget = {
    storyId: string;
    hyperlinkNodeId: string | undefined;
    href: string;
    text: string | undefined;
    targetKind: string | undefined;
    textTarget: TextSelectionTarget | null;
};
